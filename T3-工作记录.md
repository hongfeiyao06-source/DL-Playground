# T3 工作记录：DI-engine 模型代码生成器

> 分支：`feature/codegen-diengine` ｜ 工作目录：`D:/WorkSpace/dlp-T3` ｜ 代理：T3（OpenCode / glm-5.2）
> 对应任务卡章节：`docs/taskcard-v2.5.md` 的 T3。

## 一、任务概述

实现 `frontend/src/utils/codeCompileDiEngine.ts` 的 `generateDiEngineModel(nodes, edges)`（签名冻结），把网页拖拽出的神经网络图翻译成 DI-engine 合规的离散 actor-critic 模型类，并打通「TS 生成器 → node 测试写盘 → Python 读盘跑形状断言」两层测试水管。

约束：只改 `codeCompileDiEngine.ts` + `__tests__/`；唯一例外允许改 `frontend/package.json`（加 vitest devDependency 和 `test` 脚本）；不得破坏旧生成器 `codeCompile.ts`。

## 二、完成的工作

### 2.1 契约与源码研读

- 读 `git show origin/main:docs/taskcard-v2.5.md`、`git show origin/main:docs/handoff-T0.md`，确认签名契约、模型返回结构契约、两层测试要求与约束。
- 读懂旧生成器 `frontend/src/utils/codeCompile.ts` 的图→代码逻辑（`compileGraphToScript`、`generateMainCode`、`recursiveCodeGenerator`、拓扑排序、`LAYER_REGISTRY` 机制），作为参考但不改动。
- 读 DI-engine 离散模板 `ding/model/template/vac.py`（注册名 `vac`），确认三模式返回结构：
  - `compute_actor` → `{'logit': (B, action_shape)}`
  - `compute_critic` → `{'value': (B,)}`（一维，必须 squeeze）
  - `compute_actor_critic` → 两个 key 同时返回
- 读节点定义体系（`nodes/registry.ts`、`dense/LinearLayer.tsx`、`inputs/InputNode.tsx`、`pytorch_core/activations.tsx` 等），确认节点类型、`data` 结构、handles（`in-0`/`out-0`）与 shape 推导方式。

### 2.2 核心实现 `generateDiEngineModel`

文件：[frontend/src/utils/codeCompileDiEngine.ts](frontend/src/utils/codeCompileDiEngine.ts)

- **签名冻结**：`(nodes: Node[], edges: Edge[]): { model_code: string; obs_shape: number; action_shape: number }`，与 T0 桩完全一致。
- **图结构解析**：过滤 top-level 节点（无 `parentId`），做拓扑排序，统计入度/出度，识别输入节点（入度 0）与输出节点（出度 0）。
- **形状推导**：
  - `obs_shape`：输入节点 `dims` 中非 `batch` 维度尺寸的乘积；缺失时回退到首个 Linear 的 `in_features`，再兜底为 1。
  - `action_shape`：输出节点 `out_features`；缺失时回退到输出 shape 最后一维，再兜底为 1。
  - `hidden`：actor/critic 头的输入维，取输出节点 `in_features`，回退到 backbone 最后一个 `out_features`，再回退到 `obs_shape`。
- **生成模型类 `DiEngineModel`（对齐 T2 的 `user_net` 接口）**：
  - `from ding.utils import MODEL_REGISTRY` + `@MODEL_REGISTRY.register("user_net")` 自注册；`mode = ["compute_actor", "compute_critic", "compute_actor_critic"]`。
  - `__init__(self, obs_shape, action_shape, action_space="discrete")`；首个 Linear 用 `in_features=obs_shape`、actor 头用 `out_features=action_shape`，两个构造参数真正参与建模；其余层用图中字面量；`critic_head` 固定 `nn.Linear(hidden, 1)`。
  - `forward(x, mode)` 分派三模式（与 vac.py 接口一致），未知 mode 抛 `ValueError`。
  - `_encode(x)` 串起 backbone（input/输出节点除外），空 backbone 时直接 `return x`。
  - `compute_actor` 返回 `{'logit': logit}`（原始 logits，无 tanh）；`compute_critic` 返回 `{'value': critic_head(x).squeeze(-1)}`；`compute_actor_critic` 同时返回两个 key。
- **MVP 范围**：支持 `linear_layer` 与常见激活（ReLU/LeakyReLU/GELU/ELU/SELU/Tanh/Sigmoid/Softplus/Softsign/Hardswish/Hardsigmoid）；其它类型显式抛错。

### 2.3 测试（两层 + 水管）

目录：[frontend/__tests__/](frontend/__tests__/)

- `fixtures/sample_graph.json`：标准 MLP 样例图（`input(4) → Linear(4→32) → ReLU → Linear(32→2)`），两层共用。
- `diengine_codegen.test.ts`（vitest）：跑 `generateDiEngineModel(sample_graph)`，断言字符串结构（三模式分支、`logit`/`value` key、`.squeeze(-1)`、`obs_shape`/`action_shape` 构造参数、`MODEL_REGISTRY` 注册与 `mode` 属性、`action_space` 参数），并把 `model_code` 写盘到 `fixtures/sample_model.py`（另写 `fixtures/sample_shapes.json` 给 Python 侧取 shapes）。
- `diengine_model_test.py`：读 `sample_model.py`（非手写样例代码）→ `exec` → 用 `MODEL_REGISTRY["user_net"](obs_shape=..., action_shape=..., action_space="discrete")` 实例化 → dummy forward，断言 `logit (B, action_shape)`、`value (B,)`（把「生成器必须注册 user_net」这个 gap 钉进测试）。
- `fixtures/.gitignore`：忽略生成物 `sample_model.py` / `sample_shapes.json`。

### 2.4 package.json 唯一例外

文件：[frontend/package.json](frontend/package.json)

- `scripts` 增加 `"test": "vitest run"`。
- `devDependencies` 增加 `"vitest": "^4.0.0"`。

## 三、交付文件清单

| 变更类型 | 文件 |
|---------|------|
| 修改 | `frontend/src/utils/codeCompileDiEngine.ts` |
| 修改 | `frontend/package.json` |
| 修改 | `frontend/package-lock.json`（vitest 依赖树，自动生成） |
| 新增 | `frontend/__tests__/diengine_codegen.test.ts` |
| 新增 | `frontend/__tests__/diengine_model_test.py` |
| 新增 | `frontend/__tests__/fixtures/sample_graph.json` |
| 新增 | `frontend/__tests__/fixtures/.gitignore` |

未改动旧生成器 `frontend/src/utils/codeCompile.ts`。

## 四、验证结果

运行顺序（已写进交付）：**先 `npm test`（产出 fixture）→ 再 `$DING_PYTHON diengine_model_test.py`**。

```text
# npm test（vitest 4.1.11）
Test Files  1 passed (1)
     Tests  8 passed (8)

# $DING_PYTHON diengine_model_test.py
diengine_model_test: OK
obs_shape=4 action_shape=2 logit=(7, 2) value=(7,)

# npm run build（tsc -b && vite build，验证类型与旧生成器无回归）
✓ built in 1.38s
```

## 五、遇到的问题与解决方法

1. **本机 `npm` CLI 损坏**
   - 现象：`npm --version` 报 `Cannot find module 'C:\Users\Administrator\AppData\Roaming\npm\node_modules\npm\bin\npm-cli.js'`。
   - 原因：npm 全局 prefix 指向 `AppData\Roaming\npm`，但该处 `npm-cli.js` 缺失。
   - 解决：改用 `D:\nodejs\npm.cmd`（或 `node D:\nodejs\node_modules\npm\bin\npm-cli.js`）执行所有 npm 命令。

2. **沙箱网络受限，无法安装依赖**
   - 现象：`npm view vitest version` 报 `EACCES connect 198.18.0.210:443`。
   - 解决：对 `npm install --no-audit --no-fund` 走 `require_escalated` 申请联网，一次安装 240 个包成功。

3. **vitest 版本与 rolldown-vite（Vite 7）兼容性**
   - 现象：项目 `vite` 被 override 为 `npm:rolldown-vite@7.2.5`，且 `overrides` 会顺带重定向 vitest 的 vite 依赖。
   - 解决：选 `vitest@^4.0.0`（其 `@vitest/mocker` 对 vite 的 peer 范围覆盖 `^7.0.0`），实测 vitest 4.1.11 正常；未引入额外 vitest.config，复用默认 node 环境 + 现有 vite.config.ts。

4. **TypeScript 编译错误 TS6133（未使用参数）**
   - 现象：`renderModel` 的 `actionShape` 参数声明后未使用（生成代码用的是字符串字面量 `action_shape`）。
   - 解决：删除该未使用参数。

5. **首层 Linear 的 `in_features` 误用字面量而非 `obs_shape` 参数**
   - 现象：生成代码出现 `nn.Linear(in_features=4, ...)` 而非 `nn.Linear(in_features=obs_shape, ...)`，测试断言失败。
   - 原因：把初始为 `false` 的 `firstLinearSeen` 直接当作 `isFirstLinear` 传入，标志位语义反了。
   - 解决：改用 `firstLinearEmitted`，`isFirstLinear = type === "linear_layer" && !firstLinearEmitted`，并补测试断言 `in_features=obs_shape`。

6. **`DING_PYTHON` 环境变量为空**
   - 解决：按 handoff 默认路径 `D:\anaconda3\envs\ding_env\python.exe` 运行 Python 测试。

7. **T3 生成代码与 T2 `testdata/sample_model.py` 接口存在 gap**
   - 现象：T2 训练服务按注册名 `user_net` 加载模型（`from ding.utils import MODEL_REGISTRY` + `@MODEL_REGISTRY.register("user_net")` + `mode = [...]` + `action_space` 参数），而 T3 最初只生成裸类、未注册。
   - 解决：生成代码补上 `MODEL_REGISTRY` 导入、`@MODEL_REGISTRY.register("user_net")` 装饰器、`mode = [...]` 类属性、`__init__(..., action_space="discrete")`；Python 测试改为 `MODEL_REGISTRY["user_net"](...)` 实例化，把这个 gap 钉进测试防回归。

## 六、遗留问题 / 注意事项

1. **`DING_PYTHON` 未持久化**：当前环境变量为空，测试靠默认路径运行；建议后续在环境中显式设置 `DING_PYTHON=D:\anaconda3\envs\ding_env\python.exe`。
2. **MVP 仅支持 MLP**：Conv/LSTM/Attention 等节点会抛错；`obs_shape` 只按标量 int 返回（非 batch 维度之积），不适用于图像等非向量观测。
3. **多输入/多输出（分支图）未处理**：当前取第一个输入节点推导 `obs_shape`、取第一个输出节点（无出边）作为 actor 头；分支/多输出结构不在 MVP 范围。
4. **生成类名固定为 `DiEngineModel`**：现在生成代码已自带 `@MODEL_REGISTRY.register("user_net")`，T2 直接按注册名 `user_net` 取类即可，无需依赖类名；类名本身仍固定为 `DiEngineModel`。
5. **critic 头隐层维度 `hidden`**：取自输出节点 `in_features`（回退到 backbone 最后一个 `out_features`）；若输出节点不是 Linear（例如末尾接了 Softmax/激活），行为未严格定义。
6. **生成物不进版本库**：`sample_model.py` / `sample_shapes.json` 已加入 `.gitignore`，每次 `npm test` 重新生成；`dist/` 为构建产物也已 gitignore。
7. **行尾策略**：git 提示「LF 将被替换为 CRLF」，属正常换行归一化警告，不影响功能，提交时留意仓库的 `core.autocrlf` 配置。
8. **未新增 vitest.config.ts**（遵守「只改 package.json + codeCompileDiEngine.ts + __tests__/」的约束）；如后续需要 jsdom/happy-dom 或自定义 include/exclude，需新增配置文件（当前不在本任务授权范围）。

## 七、复现命令

```powershell
cd D:\WorkSpace\dlp-T3\frontend

# 1) node 测试（产出 fixtures/sample_model.py、sample_shapes.json）
& "D:\nodejs\npm.cmd" test

# 2) Python 形状断言（读生成物，非手写样例代码）
& "D:\anaconda3\envs\ding_env\python.exe" ".\__tests__\diengine_model_test.py"

# 3) 类型/旧生成器回归自检（可选）
& "D:\nodejs\npm.cmd" run build
```
